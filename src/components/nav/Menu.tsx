'use client';

import MenuBar from "@/components/icons/MenuBar";

const Menu = () => {

    return (
        <div className=''>
            <button
                onClick={() => {}}
                className='btn p-2 !w-10 !h-10'
            >
                <MenuBar className='w-full h-full' />
            </button>
        </div>
    );
};

export default Menu;